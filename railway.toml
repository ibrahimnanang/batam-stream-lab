# Batam Stream Lab — Next.js deploy (distinct from legacy Lumen LED / solar profile projects).

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm run start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
