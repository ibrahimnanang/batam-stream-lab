# Railway: Root directory mengarah ke repo ini jika mono-repo.
# Nixpacks mendeteksi Next.js secara otomatis untuk `npm install` + `npm run build`.

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm run start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
